<%= form_with(model: crust, local: true) do |form| %>
  <% if crust.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crust.errors.count, "error") %> prohibited this crust from being saved:</h2>

      <ul>
      <% crust.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :crust_name %>
    <%= form.text_field :crust_name, id: :crust_crust_name %>
  </div>

  <div class="field">
    <%= form.label :crust_price %>
    <%= form.number_field :crust_price, id: :crust_crust_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
